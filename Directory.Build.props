<Project>
  <PropertyGroup>
    <AssemblyVersion Condition="'$(VERSION)' == ''">0.0.0.0</AssemblyVersion>
    <AssemblyVersion Condition="'$(VERSION)' != ''">$(VERSION).$(BUILD_NUMBER)</AssemblyVersion>
  </PropertyGroup>
</Project>